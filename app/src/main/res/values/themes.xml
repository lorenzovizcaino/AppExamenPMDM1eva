<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AppExamenPMDM1eva" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AppExamenPMDM1eva" parent="Base.Theme.AppExamenPMDM1eva" />

    <style name="conf_botones" parent="TextAppearance.AppCompat">
        <item name="android:textColor">@color/black</item>
        <item name="backgroundTint">@color/color_botones</item>
        <item name="fontFamily">@font/alice</item>
        <item name="android:textSize">20dp</item>
    </style>
</resources>